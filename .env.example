# Copy to .env and fill in. NEVER commit .env.

# --- Database (Postgres — Neon/Supabase free tier; must be reachable by fonio's cloud) ---
# One connection string is all you need. For Neon, the simplest is the DIRECT (non-pooled) URL,
# which works for both `prisma migrate` and the app at hackathon traffic.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# --- fonio.ai (from the provisioned hackathon account; see account API docs) ---
FONIO_API_KEY=""                # Bearer token for the outbound_call API
FONIO_API_BASE_URL="https://app.fonio.ai"
FONIO_FROM_NUMBER=""            # OUTBOUND-enabled number to call FROM (E.164, e.g. +4930...). Its bound assistant runs the call.
FONIO_ASSISTANT_ID=""           # NOT used by outbound_call (assistant is bound to FROM_NUMBER); kept for reference
FONIO_WEBHOOK_SECRET=""         # to verify inbound API Request / webhook calls
FONIO_LIVE="false"              # "true" places REAL calls; "false" simulates the loop in-process
# FONIO_OUTBOUND_PATH="/api/public/v1/outbound_call"  # override only if the API path changes

# --- Live demo phones (real numbers, WITH consent) ---
DEMO_PHONE_1=""                 # EV winner — answers YES on the happy-path call
DEMO_PHONE_2=""                 # no-answer edge case — let it ring out
DEMO_PHONE_3=""                 # spare / no-consent demo patient

